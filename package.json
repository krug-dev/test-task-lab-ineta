{
  "name": "developer-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Developer portfolio landing with contact form API and AI helper",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix frontend",
    "dev:server": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "typecheck": "npm run typecheck --prefix frontend && npm run typecheck --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
